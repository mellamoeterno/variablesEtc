const plants = ['broccoli', 'cauliflower', 'cabbage', 'kale', 'tomato'];

console.log(plants.pop());


//In the code console.log(plants.pop()), 
//the parentheses () after pop are used to actually call (execute) the pop method on the plants array.

//in this case were removing and just doing console.log



//we can also remove it and give its value to another variable
//by doing:

//var ourArray = [1,2,3];
//var removedFromOurArray = ourArray.pop();

//assigning the value to the "removedFromOurArray" variable.

//.push .unshift -- .pop .shift --

//two first ones adds and the two last ones removes, 
//two adding ones returns length, will the removing ones returns value.

